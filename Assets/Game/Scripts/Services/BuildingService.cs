using System.Drawing;
using UnityEngine;

namespace Assets.Game.Scripts.Services
{
    public class BuildingService : ScriptableObject
    {
        public BuildingService()
        {
            
        }

        public void StartBuilding(Vector3 position)
        {
            //if (!IsPointFreeToBuild(point))
            //    return;

            //_builder.SetPoint();

            //_chooseBuildingUIPanel.Open(callback: buildingFactory =>
            //{
            //    _builder.SetBuilding(buildingFactory);
            //    _builder.Build();
            //});
        }
    }
}